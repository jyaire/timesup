<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Time's Up{% endblock %}</title>

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

    </head>
    <body>
    <nav class="navbar-expand navbar-light bg-light p-2">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
            <a class="navbar-brand" href="{{ path('index') }}">Time's Up</a>
            <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            </ul>
            <form class="form-inline my-2 my-lg-0">
                <a class="nav-link" href="#">
                    {% if app.user.name is defined %}
                        <a href="{{ path('team_show', {'id':app.user.id }) }}" title="Mon équipe">{{ app.user.name }}</a>
                    {% else %}
                        <a href="{{ path('app_login') }}" title="Connexion" class="nav-link">Connecte-toi</a>
                    {% endif %}
                    <span class="sr-only">(current)</span></a>
            </form>
        </div>
    </nav>


        {% block body %}{% endblock %}

        {% block javascripts %}{% endblock %}
    </body>
</html>
